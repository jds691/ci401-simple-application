package com.neo.game.input;

/**
 * A fixed list of all the possible game inputs. Used for key binding
 */
public enum Input {
    MOVE_LEFT,
    MOVE_RIGHT,
    MOVE_DOWN,

    ROTATE_LEFT,
    ROTATE_RIGHT,

    PAUSE,

    MAX
}
